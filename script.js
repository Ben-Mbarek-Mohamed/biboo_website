
document.addEventListener("DOMContentLoaded", () => {
    const btn = document.getElementById("btnCommanderPrestige");
    if (btn) {
      btn.addEventListener("click", () => {
        window.location.href = "/demande.html";  // Remplace par ta vraie URL
      });
    }
  });
  
  document.addEventListener("DOMContentLoaded", () => {
    const btn = document.getElementById("btnCommanderVisibilite");
    if (btn) {
      btn.addEventListener("click", () => {
        window.location.href = "/demande.html";  // Remplace par ta vraie URL
      });
    }
  });

  document.addEventListener("DOMContentLoaded", () => {
    const btn = document.getElementById("btnCommanderDecouverte");
    if (btn) {
      btn.addEventListener("click", () => {
        window.location.href = "/demande.html";  // Remplace par ta vraie URL
      });
    }
  });

  document.addEventListener("DOMContentLoaded", () => {
    const btn = document.getElementById("btnCommanderFidelite");
    if (btn) {
      btn.addEventListener("click", () => {
        window.location.href = "/demande.html";  // Remplace par ta vraie URL
      });
    }
  });

  document.addEventListener("DOMContentLoaded", () => {
    const btn = document.getElementById("btnDedmandez");
    if (btn) {
      btn.addEventListener("click", () => {
        window.location.href = "/demande.html";  // Remplace par ta vraie URL
      });
    }
  });

  document.addEventListener("DOMContentLoaded", () => {
    const btn = document.getElementById("btnCommencez");
    if (btn) {
      btn.addEventListener("click", () => {
        window.location.href = "/packs.html";  // Remplace par ta vraie URL
      });
    }
  });